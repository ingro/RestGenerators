<?php namespace {{ namespace }}\Transformers;

// Automatically generated by Illusion Generator on {{ "now"|date("d/m/Y - H:i:s", "Europe/Paris") }}

use Ingruz\Rest\Transformers\RestTransformer;
use {{ namespace }}\Models\{{ className }};

class {{ className }}Transformer extends RestTransformer{

    public function transform({{ className }} $item)
    {
        return [
            'id' => (int) $item->id,
        {% for field, type in fields %}
            '{{ field }}' => {% if type == 'integer' %}(int) {% endif %}$item->{{ field }},
        {% endfor %}
        ];
    }
}