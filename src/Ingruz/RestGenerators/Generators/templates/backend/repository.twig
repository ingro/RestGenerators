<?php namespace {{ namespace }}\Repositories;

// Automatically generated by Illusion Generator on {{ "now"|date("d/m/Y - H:i:s", "Europe/Paris") }}

use Ingruz\Rest\Repositories\AbstractRestEloquentRepository;
use {{ namespace }}\Models\{{ className }};

class {{ className }}Repository extends AbstractRestEloquentRepository {

    public function __construct({{ className }} $item)
    {
        $this->model = $item;
    }
}