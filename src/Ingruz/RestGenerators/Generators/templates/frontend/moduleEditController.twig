define (require) ->
    App = require 'app'
    Views = require 'modules/{{ className }}/{{ className }}_views'
    require 'entities/{{ className }}'

    class {{ className }}EditController extends App.Base.Controllers.Edit
        Views: Views